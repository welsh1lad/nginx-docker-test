
  node { 
       label 'docker'
             
         stage('Build') {
                 steps {
                     echo 'Hi, GeekFlare. Starting to build the App.'
                       }
                 }
  }
